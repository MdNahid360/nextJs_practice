/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
   images: {
    domains: ["images.pexels.com", "static.vecteezy.com","svgrepo.com"],
  },
}

module.exports = nextConfig
